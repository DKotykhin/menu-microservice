services:
  menu-app:
    container_name: menu-app
    image: menu-microservice:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 5001:5001
    env_file:
      - ./.env.docker
    networks:
      - coffeedoor-network

networks:
  coffeedoor-network:
      external: true
